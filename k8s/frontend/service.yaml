apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: ai-coach-prod
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort # Sử dụng NodePort để kết nối với GCE Ingress
